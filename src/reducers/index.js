import {combineReducers} from 'redux';
import globals from './global';

/**
 *
 * The main app reducer combines other reducers in the reducers module
 *
 */

export default combineReducers({
	globals
	
});
